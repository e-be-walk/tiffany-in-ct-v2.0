<h1>Window Entries:</h1>
<div class="row">
  <% @windows.each do |window| %>
  <div class="col-md-3">
  <%= image_tag window.image.url(:medium) %><br>
  <h4><%= link_to window.name, site_window_path(window.site_id, window) %><h4>
  </div>
  <% end %>
</div>
